# =============================================================================
# Bifrost - fail2ban jail.local reference
#
# Runtime configuration is generated by scripts/security.sh:setup_fail2ban and
# written to /etc/fail2ban/jail.local on target hosts.
#
# Edit scripts/security.sh for operational changes. This static file stays as a
# short reference so package templates do not drift from the single runtime source.
# =============================================================================
